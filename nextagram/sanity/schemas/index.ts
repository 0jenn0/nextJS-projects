import author from "./author";
import project from "./project";
import post from "./post";
import verificationToken from "./verification-token";

const schemas = [author, project, post, verificationToken];

export default schemas;
